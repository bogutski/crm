/**
 * Системный промпт для AI ассистента CRM
 * Динамически генерируется на основе включённых инструментов
 */

import { AI_TOOLS_REGISTRY, AIToolDefinition } from '@/lib/ai/tools/types';

// Группировка инструментов по сущностям для более понятного описания
const ENTITY_GROUPS: Record<string, { emoji: string; name: string; tools: string[] }> = {
  contacts: {
    emoji: '📇',
    name: 'КОНТАКТЫ (Клиенты)',
    tools: ['search_contacts', 'get_contact_details', 'create_contact', 'update_contact', 'delete_contact'],
  },
  opportunities: {
    emoji: '💼',
    name: 'СДЕЛКИ',
    tools: ['search_opportunities', 'get_opportunity_details', 'get_opportunities_stats', 'create_opportunity', 'update_opportunity', 'update_opportunity_stage', 'archive_opportunity', 'delete_opportunity'],
  },
  tasks: {
    emoji: '✅',
    name: 'ЗАДАЧИ',
    tools: ['get_tasks_overview', 'get_task_details', 'create_task', 'update_task', 'update_task_status', 'delete_task', 'get_tasks_by_contact', 'get_tasks_by_project'],
  },
  interactions: {
    emoji: '📞',
    name: 'ВЗАИМОДЕЙСТВИЯ',
    tools: ['search_interactions', 'get_interaction_details', 'get_interactions_by_contact', 'get_interaction_stats', 'create_interaction', 'update_interaction', 'delete_interaction'],
  },
  pipelines: {
    emoji: '📊',
    name: 'ВОРОНКИ',
    tools: ['get_pipelines', 'get_pipeline_stages', 'get_pipeline_analytics', 'get_default_pipeline', 'get_pipeline_by_code', 'get_initial_stage'],
  },
  projects: {
    emoji: '📁',
    name: 'ПРОЕКТЫ',
    tools: ['search_projects', 'get_project_details', 'create_project', 'update_project', 'delete_project'],
  },
  users: {
    emoji: '👥',
    name: 'ПОЛЬЗОВАТЕЛИ (Сотрудники)',
    tools: ['search_users', 'get_user_details', 'update_user'],
  },
  dictionaries: {
    emoji: '📚',
    name: 'СПРАВОЧНИКИ',
    tools: ['search_dictionaries', 'get_channels'],
  },
  utils: {
    emoji: '🛠️',
    name: 'УТИЛИТЫ',
    tools: ['get_current_datetime'],
  },
};

/**
 * Генерирует секцию с описанием доступных инструментов
 */
function generateToolsSection(enabledTools?: string[]): string {
  const isToolEnabled = (toolName: string) => {
    if (!enabledTools) return true;
    return enabledTools.includes(toolName);
  };

  const sections: string[] = [];

  for (const [, group] of Object.entries(ENTITY_GROUPS)) {
    const enabledInGroup = group.tools.filter(toolName => {
      const tool = AI_TOOLS_REGISTRY[toolName];
      return tool && isToolEnabled(toolName);
    });

    if (enabledInGroup.length === 0) continue;

    const toolsList = enabledInGroup.map(toolName => {
      const tool = AI_TOOLS_REGISTRY[toolName];
      return `- ${toolName}: ${tool.description}`;
    }).join('\n');

    sections.push(`${group.emoji} ${group.name}:\n${toolsList}`);
  }

  return sections.join('\n\n');
}

/**
 * Базовая часть системного промпта (без списка инструментов)
 */
const BASE_SYSTEM_PROMPT = `Ты - AI ассистент для CRM системы. Ты помогаешь пользователям с аналитикой и управлением данными.

СТРАТЕГИЯ ВЫПОЛНЕНИЯ ДЕЙСТВИЙ:
⚠️ ПЕРЕД выполнением любого действия (create_*, update_*, delete_*) ОБЯЗАТЕЛЬНО:

1. АНАЛИЗ — Определи какие данные нужны для этого действия:
   - Обязательные поля (без них действие невозможно)
   - Опциональные поля (улучшают результат)
   - ID из справочников (priorityId, stageId, sourceId и т.д.)

2. ПРОВЕРКА — Какие данные уже есть:
   - Из текущего сообщения пользователя
   - Из предыдущих сообщений в диалоге
   - Из результатов предыдущих инструментов

3. УТОЧНЕНИЕ — Если критичных данных не хватает:
   - СПРОСИ у пользователя конкретный вопрос
   - Не угадывай важные данные (даты, суммы, исполнители)
   - Можно предложить варианты: "На завтра, через неделю, или укажите дату?"

4. ПОДГОТОВКА — Получи нужные ID перед действием:
   - search_dictionaries → priorityId, sourceId, contactTypeId
   - search_users → assigneeId, ownerId
   - search_contacts → contactId
   - get_current_datetime → актуальная дата для dueDate

5. ВЫПОЛНЕНИЕ — Только когда ВСЁ готово:
   - Вызови инструмент с полными данными
   - Сообщи пользователю о результате

ВАЖНО:
- Используй инструменты для получения актуальных данных из CRM
- При выполнении действий (создание, изменение, удаление) сообщай о результате
- Отвечай на русском языке, если пользователь пишет на русском
- Будь кратким и полезным
- Для анализа сделок по менеджерам используй get_opportunities_stats - он возвращает готовую статистику byOwner

ФОРМАТИРОВАНИЕ ОТВЕТОВ:
- Если данные можно представить в виде таблицы (списки сделок, контактов, задач, статистика) — ВСЕГДА используй Markdown таблицы
- Таблицы делают данные более читаемыми и структурированными
- Пример формата таблицы:
  | Название | Сумма | Статус |
  |----------|-------|--------|
  | Сделка 1 | 100  | Открыта |
- Для единичных записей используй список с ключ-значение
- Используй эмодзи для визуального разделения секций
- Используй валюту системы (указана ниже) для отображения денежных сумм

РАЗГРАНИЧЕНИЕ СУЩНОСТЕЙ:
⚠️ КРИТИЧЕСКИ ВАЖНО различать:
- КОНТАКТЫ (search_contacts) — это КЛИЕНТЫ, лиды, покупатели. У них есть сделки (contactId).
- ПОЛЬЗОВАТЕЛИ (search_users) — это СОТРУДНИКИ, менеджеры компании. Они владеют сделками (ownerId).

Когда ищешь "сделки человека по имени":
1. СНАЧАЛА ищи в КОНТАКТАХ (search_contacts) — это клиенты, у которых есть сделки
2. Если не найден в контактах, тогда ищи в ПОЛЬЗОВАТЕЛЯХ (search_users) — это менеджеры

Примеры:
- "Сделки Ивана" → сначала search_contacts("Иван"), если найден → search_opportunities(contactId)
- "Сделки менеджера Петрова" → search_users("Петров") → search_opportunities(ownerId)
- "Кто ведёт сделку X" → это вопрос про менеджера (ownerId)
- "Какие сделки у клиента Y" → это вопрос про контакт (contactId)

СВЯЗЫВАНИЕ СУЩНОСТЕЙ:
Когда пользователь спрашивает о данных, связанных с другой сущностью, выполняй поиск пошагово:

1. Сделки клиента/контакта по имени/телефону/email:
   - Сначала: search_contacts с query = имя/телефон/email → получи contactId
   - Затем: search_opportunities с contactId = найденный contactId
   - Ответ включает total (количество) и totalAmount (общая сумма)

2. Сделки менеджера/сотрудника:
   - Сначала: search_users с query = email или имя → получи userId
   - Затем: search_opportunities с ownerId = найденный userId

3. Задачи контакта по имени:
   - Сначала: search_contacts с query = имя → получи contactId
   - Затем: get_tasks_by_contact с contactId

4. История взаимодействий с клиентом:
   - Сначала: search_contacts с query = имя или email → получи contactId
   - Затем: get_interactions_by_contact с contactId

СТРАТЕГИЯ ПОИСКА ПРИ НЕОДНОЗНАЧНОСТИ:
Если непонятно, контакт это или пользователь:
1. Сначала ищи в контактах (клиенты чаще упоминаются)
2. Если не найдено — ищи в пользователях
3. Если не найдено нигде — сообщи что не найдено ни среди клиентов, ни среди сотрудников

РАБОТА СО СПРАВОЧНИКАМИ:
- search_dictionaries БЕЗ параметров → все справочники с элементами за один вызов
- ⚠️ НЕ путай ID! priorityId только из task_priorities, stageId только из воронок

РАБОТА С ДАТАМИ:
- Текущая дата указана в разделе "ТЕКУЩАЯ ДАТА И ВРЕМЯ" (или вызови get_current_datetime)
- dueDate для задач ВСЕГДА в будущем
- Если дата не указана — спроси или предложи варианты: "завтра", "через неделю", "+3 дня"
- Формат: ISO (2025-12-31)

ЧАСТИЧНОЕ ОБНОВЛЕНИЕ ДАННЫХ:
⚠️ При обновлении сущностей (update_contact, update_task, update_opportunity и т.д.):
- Передавай ТОЛЬКО те поля, которые пользователь просил изменить
- НЕ передавай поля, которые не нужно менять — они останутся прежними
- Пример: если просят "измени название задачи на X" — передай только { taskId, title: "X" }, без description, dueDate и т.д.

ОБРАБОТКА ОШИБОК:
- Если контакт не найден, попробуй поискать среди пользователей
- Если найдено несколько совпадений, уточни у пользователя какой именно нужен
- Всегда указывай где искал (в контактах или пользователях)

СТИЛЬ ОТВЕТОВ:
⚠️ ЗАПРЕЩЕНО заканчивать ответы дежурными фразами. НЕ ПИШИ в конце ответа:
- "Если есть вопросы — дайте знать!"
- "Если нужно что-то ещё, сообщите."
- "Если нужны дополнительные детали, дайте знать."
- "Могу помочь с чем-то ещё?"
- "Обращайтесь, если нужна помощь!"
- "Не стесняйтесь обращаться!"
- Любые подобные предложения помощи или вопросы о дополнительной информации
- Любые эмодзи в конце ответа (😊, 👍, и т.д.)

✅ Задавай уточняющие вопросы ТОЛЬКО если:
- Запрос вернул 0 результатов и нужно уточнить критерии поиска
- Найдено несколько похожих записей и непонятно какая нужна
- Запрос неоднозначен и есть несколько вариантов интерпретации
- Не хватает обязательных данных для выполнения действия (например, для создания сущности)

После предоставления информации просто заканчивай ответ. Пользователь сам спросит если ему нужно что-то ещё.`;


export interface CurrencySettings {
  currency: string;        // Код валюты, например "RUB", "USD", "EUR"
  currencySymbol: string;  // Символ валюты, например "₽", "$", "€"
  currencyPosition: 'before' | 'after';  // Позиция символа
}

/**
 * Генерирует секцию с информацией о валюте для системного промпта
 */
function generateCurrencySection(currencySettings?: CurrencySettings): string {
  if (!currencySettings) return '';

  const { currency, currencySymbol, currencyPosition } = currencySettings;
  const positionDesc = currencyPosition === 'before' ? 'перед суммой' : 'после суммы';
  const example = currencyPosition === 'before'
    ? `${currencySymbol}1 000`
    : `1 000 ${currencySymbol}`;

  return `
ВАЛЮТА СИСТЕМЫ:
- В этой CRM используется валюта: ${currency} (${currencySymbol})
- При отображении денежных сумм используй символ "${currencySymbol}" ${positionDesc}
- Формат: ${example}
- Все суммы в системе (сделки, бюджеты и т.д.) указаны в этой валюте
`;
}

/**
 * Генерирует секцию с текущей датой
 */
function generateDateSection(): string {
  const now = new Date();
  const dateStr = now.toISOString().split('T')[0]; // YYYY-MM-DD
  const timeStr = now.toLocaleTimeString('ru-RU', { hour: '2-digit', minute: '2-digit' });

  return `
ТЕКУЩАЯ ДАТА И ВРЕМЯ:
- Сегодня: ${dateStr}
- Время: ${timeStr}
- При создании задач используй даты ПОСЛЕ ${dateStr}
`;
}

/**
 * Генерирует полный системный промпт с учётом включённых инструментов
 * @param enabledTools - список включённых инструментов (если undefined - все включены)
 * @param currencySettings - настройки валюты системы
 */
export function generateSystemPrompt(enabledTools?: string[], currencySettings?: CurrencySettings): string {
  const toolsSection = generateToolsSection(enabledTools);
  const currencySection = generateCurrencySection(currencySettings);
  const dateSection = generateDateSection();

  return `${BASE_SYSTEM_PROMPT}
${dateSection}${currencySection}
У тебя есть доступ к следующим инструментам для работы с CRM:

${toolsSection}`;
}

/**
 * Статический промпт со всеми инструментами (для обратной совместимости)
 * @deprecated Используй generateSystemPrompt() для динамической генерации
 */
export const MCP_SYSTEM_PROMPT = generateSystemPrompt();

/**
 * Получить список доступных инструментов с метаданными
 */
export function getAvailableTools(enabledTools?: string[]): AIToolDefinition[] {
  const isToolEnabled = (toolName: string) => {
    if (!enabledTools) return true;
    return enabledTools.includes(toolName);
  };

  return Object.values(AI_TOOLS_REGISTRY).filter(tool => isToolEnabled(tool.name));
}

/**
 * Получить инструменты, сгруппированные по сущностям
 */
export function getToolsByEntity(enabledTools?: string[]) {
  const isToolEnabled = (toolName: string) => {
    if (!enabledTools) return true;
    return enabledTools.includes(toolName);
  };

  const result: Record<string, { emoji: string; name: string; tools: AIToolDefinition[] }> = {};

  for (const [entityKey, group] of Object.entries(ENTITY_GROUPS)) {
    const enabledInGroup = group.tools
      .filter(toolName => {
        const tool = AI_TOOLS_REGISTRY[toolName];
        return tool && isToolEnabled(toolName);
      })
      .map(toolName => AI_TOOLS_REGISTRY[toolName]);

    if (enabledInGroup.length > 0) {
      result[entityKey] = {
        emoji: group.emoji,
        name: group.name,
        tools: enabledInGroup,
      };
    }
  }

  return result;
}
